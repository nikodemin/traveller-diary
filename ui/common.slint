export enum State { main_view, travels_view, photos_view}

export global GridViewSettings {
    out property <int> num-cols;
    in property <length> min-el-width: 200px;
    in property <length> max-el-width: 300px;
}